{
  "name": "ecommerce-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Vanilla JS frontend, no build step — served as static files",
  "scripts": {
    "dev": "http-server src -p 8080 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}