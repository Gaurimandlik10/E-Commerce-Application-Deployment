apiVersion: v1
kind: Service
metadata:
  name: node-service
  namespace: ecommerce
  labels:
    app: node-backend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: node-backend
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}