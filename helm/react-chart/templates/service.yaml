apiVersion: v1
kind: Service
metadata:
  name: react-service
  namespace: ecommerce
spec:
  type: {{ .Values.service.type }}
  selector:
    app: react-frontend
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}